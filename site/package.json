{
  "name": "whoshowsup",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "observable preview --port 3000 --no-open",
    "build": "observable build",
    "clean": "rimraf src/.observablehq/cache"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.4",
    "us-atlas": "^3.0.1",
    "topojson-client": "^3.1.0"
  }
}
